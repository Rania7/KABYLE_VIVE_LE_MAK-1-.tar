embedding_dim: 128  # Embedding dimension
hidden_dim: 128     # Hidden dimension (nbr neurone in neural network layer)
num_layers: 1       # Number of GRU stacks' layers (type oF RNN THAT is effective in sequential data (time series)
num_gnn_layers: 5   # Number of GNN layers
dropout_prob: 0.0   # Droput rate
batch_norm: false   # Batch normalization
l2reg_rate: 2.5e-4  # L2 regularization rate
